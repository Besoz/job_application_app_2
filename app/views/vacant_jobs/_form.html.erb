<%= form_for(@vacant_job) do |f| %>
  <% if @vacant_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vacant_job.errors.count, "error") %> prohibited this vacant_job from being saved:</h2>

      <ul>
      <% @vacant_job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
